{
  "name": "cocopo",
  "version": "1.5.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run rbuild & npm run mbuild",
    "rbuild": "tsc --project src/render",
    "mbuild": "tsc --project src/main",
    "start": "electron App",
    "release": "electron-packager App CocoPo --platform=win32 --arch=x64 --electron-version 5.0.6 --icon=icon.ico --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hiroki",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "electron": "^5.0.6"
  }
}
